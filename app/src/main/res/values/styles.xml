<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Welcome & Login Theme. -->
    <style name="AppThemeAlternative" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimaryWelcome</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkWelcome</item>
        <item name="colorAccent">@color/colorPrimaryButton</item>

        <item name="android:textColorHint">@color/darkGray</item>
        <item name="colorControlNormal">@color/darkGray</item>
        <item name="colorControlActivated">@color/darkGray</item>
    </style>

    <style name="LinearProgress" parent="Theme.AppCompat.Light">
        <item name="colorAccent">@color/themeColor</item>
        <item name="android:progressBackgroundTint" tools:targetApi="lollipop">
            @android:color/darker_gray
        </item>
    </style>
</resources>
